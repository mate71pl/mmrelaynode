name: Build Meshtastic firmware

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python 3
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get -y install wget g++ zip ca-certificates
          pip install --upgrade pip
          pip install platformio

      - name: Clone Meshtastic repository and compile firmware
        run: |
          git clone https://github.com/meshtastic/firmware --recurse-submodules /tmp/firmware
          cd /tmp/firmware
          chmod +x ./bin/build-native.sh
          ./bin/build-native.sh
          cd /tmp/firmware/release/
          
      - name: Upload firmware to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: meshtasticd_linux_amd64
          asset_name: meshtasticd_linux_amd64
          tag: ${{ github.ref }}
          overwrite: true
          body: "Meshtastic native"
      
